[pytest]
python_files = test_*.py
testpaths = tests
addopts = -q --headed --browser chromium

markers =
    smoke: smoke tests
    regression: regression tests
    e2e: end-to-end tests
    api: api tests